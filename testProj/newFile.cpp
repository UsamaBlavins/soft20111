#include <iostream>

struct Node {
	int data;
	Node* next;
};

int main() {    
	Node *list, *current, *first, *last, *temp, *trail, *p, *q;
	list = new Node;
	list->data = 47;
	temp = list;
	first = list;
	list = new Node;
	
	return 0;
}
